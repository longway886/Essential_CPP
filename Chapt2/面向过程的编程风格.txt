函数定义必须的四个部分：
  (1) 返回类型
  (2) 函数名
  (3) 参数列表(parameter list)
  (4) 函数体

函数声明需要的部分
  必须指明返回类型、函数名以及参数列表，不必提供函数体。此即所谓的函数原型(function prototype)

当我们调用一个函数时，会在内存中建立起一块特殊区域，称为“程序堆栈(program stack)”。
它提供了函数定义的每个对象的内存空间——我们将这些对象称为local object（局部对象）。
一旦函数完成，这块内存就会被释放掉，或者说是从程序堆栈中被pop出来。